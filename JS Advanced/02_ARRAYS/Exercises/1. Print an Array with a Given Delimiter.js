function solve(inputArray, delimiter) {
    console.log(inputArray.join(delimiter));
}

solve(['One', 
'Two', 
'Three', 
'Four', 
'Five'], 
'-'
);

solve(['How about no?', 
'I',
'will', 
'not', 
'do', 
'it!'], 
'_'
);